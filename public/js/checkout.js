document.addEventListener("DOMContentLoaded", () => {
    console.log("Checkout page loaded.");

    // Add any JavaScript needed for the checkout process here
    // (e.g., validation, button actions, etc.)
});
